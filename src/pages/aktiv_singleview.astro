---
import MainLayout from "../layouts/MainLayout.astro";
import { Image } from "astro:assets";
import Heading from "../components/Heading.astro";
import Text from "../components/Text.astro";
import Button from "../components/Button.astro";
import Link from "../components/Link.astro";

// Get data from URL params (sent from Card component)
const url = new URL(Astro.request.url);
const title = url.searchParams.get("title") || "Default Title";
const image = url.searchParams.get("image") || "https://placehold.co/1200x600";
---

<MainLayout title={title}>
  <main class="container mx-auto p-4">
    <!-- Back Link -->
    <div class="mb-6 animate-fade-in">
      <Link href="/card-example">← Tilbage til oversigt</Link>
    </div>

    <!-- Hero Image -->
    <div
      class="mb-8 rounded-lg overflow-hidden shadow-lg animate-fade-in animate-delay-100"
    >
      <img src={image} alt={title} class="w-full h-64 md:h-96 object-cover" />
    </div>

    <!-- Content -->
    <article class="max-w-3xl mx-auto animate-fade-in animate-delay-200">
      <Heading htmlheading="h1" size="large" stylingclasses="mb-6">
        {title}
      </Heading>

      <Text teksttype="brødtekst">
        Dette er single view siden. Her ville du normalt vise detaljeret indhold
        baseret på det kort, brugeren klikkede på.
      </Text>

      <Text teksttype="brødtekst">
        I en reel implementation ville du hente data fra en API eller CMS
        baseret på et ID, eller sende mere kompleks data gennem Astro's routing
        system.
      </Text>

      <div class="mt-8">
        <Link href="/bliv_aktiv" variant="primary"> Tilmeld dig nu </Link>
      </div>
    </article>
  </main>
</MainLayout>
